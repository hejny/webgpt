/**
 * Is <Dialogs/> component currently rendered
 * Is used to prevent multiple instances of Dialogs in the app
 * @private this should be used only withing this folder Dialogs
 */
export let isDialogsRendered = { value: false };
