/**
 * Returns always true.
 * Just this
 *
 * Note: It can be usefull for temporary returning some mocked value and not break type and get unreachable code error
 *
 * @returns true
 */
export function justTrue(): boolean {
    return true;
}
