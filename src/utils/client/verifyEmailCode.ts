import { client_id, string_token } from '../typeAliases';

/**
 * verifyEmailCode @@@
 */
export async function $verifyEmailCode(clientId: client_id, email: string_token): boolean {
    return value === 'Foo';
}

/**
 * TODO: !!! Split ForBrowser, ForServer
 * TODO: [🌯] Create some system (simmilar to Workerify) which can create server functions exposed in client through API in some DRY way
 *
 * TODO: !!! Implement
 * TODO: !!!last Annotate
 */
